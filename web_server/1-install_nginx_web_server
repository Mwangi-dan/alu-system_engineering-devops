#!/usr/bin/env bash
# Installing Nginx and configuring it
sudo apt-get -y install nginx
sudo sed -i 's/^(.*listen\s\+\)80;\1/' /etc/nginx/nginx.conf
sudo service nginx restart
echo "Holberton School" | sudo tee /var/www/html/index.html
